# ── Railway (backend) ──────────────────────────────────────────────────────────
# Set these in the Railway service's environment variables UI.

# Gemini API key for backend emotion detection, director agent, and content pipeline
GOOGLE_API_KEY=your-gemini-api-key-here

# Comma-separated list of allowed CORS origins (set to your Netlify site URL after deploy)
# Example: CORS_ALLOWED_ORIGINS=https://my-directors-cut.netlify.app
CORS_ALLOWED_ORIGINS=http://localhost:3000

# ── Netlify (frontend) ─────────────────────────────────────────────────────────
# Set these in the Netlify site's environment variables UI (not committed).

# Gemini API key used by the browser Gemini Live hook (client-side)
VITE_GEMINI_API_KEY=your-gemini-api-key-here

# Full URL of the Railway backend (no trailing slash).
# When set, WebSocket connections go directly to Railway instead of same-origin.
# Example: VITE_BACKEND_URL=https://my-directors-cut.up.railway.app
# Leave unset for local Docker Compose (same-origin fallback).
VITE_BACKEND_URL=
