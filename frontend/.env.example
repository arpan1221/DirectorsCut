# Gemini API key — baked into the React bundle at build time.
# Copy to .env.local for local `npm run dev`.
# For Docker, set GOOGLE_API_KEY in the root .env file and docker-compose
# passes it through as the VITE_GEMINI_API_KEY build arg.
VITE_GEMINI_API_KEY=your-gemini-api-key-here
